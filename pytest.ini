[tool:pytest]
testpaths = tests
addopts = -v --tb=short
python_files = test_*.py
python_classes = Test*
python_functions = test_*

[pytest]
# 设置Python路径以确保模块能正确导入
pythonpath = . tests tests/rpa

# 添加其他配置
filterwarnings = ignore::DeprecationWarning
